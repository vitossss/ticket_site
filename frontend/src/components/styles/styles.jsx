import {createTheme, styled} from "@mui/material/styles";

const Colors = {

}

const theme = createTheme({
    palette: {
        primary: {
            main: '#001d3d'
        }
    },
});



export default theme;
